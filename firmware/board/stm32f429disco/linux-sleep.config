# The external uClibc toolchain uses 32-bit time_t and therefore calls the
# legacy time syscall entry points. Keep them out of the minimal image unless
# the sleep/time API example is selected.
CONFIG_COMPAT_32BIT_TIME=y

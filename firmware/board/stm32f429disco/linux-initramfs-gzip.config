# Store the embedded initramfs compressed in flash. The kernel remains XIP and
# expands only the root filesystem archive into RAM during boot.
CONFIG_RD_GZIP=y
# CONFIG_INITRAMFS_COMPRESSION_NONE is not set
CONFIG_INITRAMFS_COMPRESSION_GZIP=y

# Gallery and SD-card images omit networking unless another selected example
# requires it. Keeping this separate from linux-compact.config makes that
# policy explicit and avoids overriding Find My Device's network fragment.
# CONFIG_NET is not set

config BR2_PACKAGE_HELLO_CPP
	bool "hello-cpp"
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  Build the C++ Makefile and configuration-file teaching project.

comment "hello-cpp requires C++ support in the toolchain"
	depends on !BR2_INSTALL_LIBSTDCPP
